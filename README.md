请读取 db2-data-toolkit-plain.txt，将其完整恢复成原来的项目结构。

要求：
1. 新建 db2-data-toolkit 文件夹；如果已存在且非空，使用新的目录，不能覆盖已有文件。
2. 每个文件区块的格式为：
   ===== FILE: 相对路径 =====
   UTF8-BYTES: 字节数
   SHA256: 校验值
   ===== CONTENT =====
   文件原始内容
   ===== END FILE =====
3. 按相对路径创建子目录和文件。所有文件必须位于新建的项目目录内。
4. 从 CONTENT 标记后的第一个字节开始，读取 UTF8-BYTES 指定数量的字节作为文件内容。这是 UTF-8 字节数，不是字符数。
5. 不要把标记、元数据或 END FILE 前额外添加的分隔换行写入文件。保留原来的编码、换行和内容。
6. 不要修改、重构、补全或省略任何代码。可以编写本地脚本自动拆分。
7. 应恢复 176 个文件，逐个验证字节数和 SHA256。若不匹配，报告具体文件，不要擅自修改内容来绕过校验。
8. 校验通过后，在恢复目录执行 mvn package。明确关闭以下数据库测试：
   DB2_VALIDATION_INTEGRATION=false
   DB2_VALIDATION_FIXTURES=false
   DB2_VALIDATION_SCALE=false
9. 不连接、不修改数据库，不自动运行造数程序。
10. 最后报告恢复目录、文件数量、校验结果、构建和测试结果，以及生成的 JAR 路径。

注意：TXT 不包含二进制 baseline Excel。不要伪造这些文件；它们不影响编译，但运行依赖 baseline 的比较前需要另外准备。












=================================
